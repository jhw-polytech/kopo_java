package kr.ac.kopo.jdbc;

public class ArrayBoardHint {
	
	public static void main(String[] args) {
		

		/*
		
		
		BoardVO[] boardArr = new BoardVO[5];
		int loc = 0;
		if(loc == boardArr.length) {
			BoardVO[] temp = new BoardVO[loc+5]l
			boardArr = temp;		
		}


		// 실제 보드 VO 객체가 따로 만들어지는 게 아니라, 참조하는 게 달라지는 것
		// 따라서 생각보다 메모리가 왔다갔다 하지 않는다.-ㅔ-[.,
		// stack의 메모리가 한정되어 있기 때문에, heap을 사용하려고 하는 것임

		
		*/
		
	}

}
